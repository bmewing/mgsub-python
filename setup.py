from setuptools import setup

setup(
   name='mgsub',
   version='1.0',
   description='A safe, multiple, simultaneous string substitution function',
   author='Mark Ewing',
   packages=['mgsub'],  #same as name
   install_requires=[], #no external packages as dependencies as of now
   scripts=[], #no scripts as of now
)
